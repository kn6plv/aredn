{% if (request.env.REQUEST_METHOD === "PUT") {
    configuration.prepareChanges();
%}
{{ _R("changes") }}
{% return; } %}
<dialog>
    {{ _R("dialog-header", "Tunnels") }}
    <div>
    </div>
    {{ _R("dialog-footer") }}
</dialog>
