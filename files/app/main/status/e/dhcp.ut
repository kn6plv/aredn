{% if (request.env.REQUEST_METHOD === "PUT") {
    settings.prepareChanges();
%}
{{ _R("changes") }}
{% return; } %}
<dialog>
    {{ _R("dialog-header", "DHCP") }}
    <div>
    </div>
    {{ _R("dialog-footer") }}
</dialog>
